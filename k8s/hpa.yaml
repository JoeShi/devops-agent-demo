apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: outline-web
  namespace: outline
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: outline-web
  minReplicas: 3
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
