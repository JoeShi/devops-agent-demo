apiVersion: apps/v1
kind: Deployment
metadata:
  name: outline-worker
  namespace: outline
  labels:
    app: outline
    component: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: outline
      component: worker
  template:
    metadata:
      labels:
        app: outline
        component: worker
    spec:
      containers:
        - name: outline-worker
          image: 604179600882.dkr.ecr.us-east-1.amazonaws.com/outline:c55e0983ec0fbce4da9b3a2626b38188f3215f52
          command: ["node", "build/server/index.js", "--services=worker"]
          envFrom:
            - secretRef:
                name: outline
          env:
            - name: URL
              value: "https://outline.devops-agent.xyz"
            - name: FILE_STORAGE
              value: "local"
            - name: PGSSLMODE
              value: "require"
            - name: NODE_TLS_REJECT_UNAUTHORIZED
              value: "0"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
